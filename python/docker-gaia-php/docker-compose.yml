version: "3.9"

services:
    web:
        image: nginx:latest
        ports:
            - "8666:80"
        volumes:
            - ./src:/var/www/html
            - ./default.conf:/etc/nginx/conf.d/default.conf
        restart: always
        links:
            - php-fpm

    php-fpm:
        build: docker/php-fpm
        volumes:
            - ./src:/var/www/html
        restart: always
