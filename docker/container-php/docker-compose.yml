version: "3.9"

services:
    web:
        image: nginx:latest
        ports:
            - "8666:80"
        volumes:
            - ./app:/var/www/html
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
        restart: always
        links:
            - php-fpm

    php-fpm:
        build: ../image-php-fpm
        volumes:
            - ./app:/var/www/html
        restart: always
